<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.junade.animaltimer">

    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Required for apps targeting Android 13+ (API 33) that use the Advertising ID.
         Without this permission, the Advertising ID returns a string of zeros.
         AnimalTimer uses Google Mobile Ads (AdMob), so this permission is required. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <!-- Required for Google Play Billing (in-app purchases).
         The in_app_purchase plugin normally merges this permission automatically,
         but we declare it explicitly to be safe and to make the dependency visible. -->
    <uses-permission android:name="com.android.vending.BILLING"/>

    <application
        android:label="AnimalTimer"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>


        <!-- AdMob App ID (test ID) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7203301690798915~6954811849"/>

        <meta-data android:name="flutterEmbedding" android:value="2"/>
    </application>
</manifest>
